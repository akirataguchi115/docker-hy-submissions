version: '3.5'

services:
    backend:
      image: example-backend
      environment:
        - REDIS_HOST=redis
      ports:
      -  8080:8080 
    frontend:
      image: example-frontend
      ports:
      -  5000:5000
    redis:
      image: redis
      ports:
      -  6379:6379
